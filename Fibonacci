#include<stdio.h>
int fib(int n)
int main(){
	int x;
	scanf("%d",&x);
	for(int i=0;i<n;i++){
		printf("%5d %7d",i,fib(i));
		printf("\n");

	}	return 0;
}
int fib(int n){
	if(n<=2)
		return 1;
	else
		return fib(n-1)+fib(n-2);
}
