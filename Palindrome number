#include<stdio.h>
main(){
	int n,d,r,x;
	
	scanf("%d",&n);
	x=n;
	while(n!=0){
		d=n%10;
		r=r*10+d;
		n=n/10;
	}
	printf("Reverse number=%d\n",r);
	if(r==x)
		printf("Palindrome");
	else
		printf("Not a palindrome");	
}
